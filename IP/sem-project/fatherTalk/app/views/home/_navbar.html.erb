<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <a id="navbar-logo" class="navbar-brand">Беседы с батюшкой</a>
    <!-- <a class="navbar-brand">Беседы с батюшкой</a> -->
    <div class="collapse navbar-collapse" id="navbarCollapse">
        <form class="form-inline mt-2 mt-md-0" action="{% url 'ask_question' %}">
            <input class="form-control mr-sm-2" type="text" placeholder="Найти путь к Богу.." aria-label="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Исповедоваться</button>
        </form>
    </div>
<!--     {% if request.user.is_authenticated %}
    <img id="navbar_avatar_image" src="{{ request.user.profile.image.url }}" alt="My image">
    {% endif %} -->
    <div>
        <div class="profile_info">
            <span id="name"> <!-- {{ request.user.username }} -->Ivan </span>
            <div class="header_profile_links">
                <!-- {% if request.user.is_authenticated %} -->
                <% if current_user %>
                    <%= link_to 'Log Out', logout_path %> 
                <% else %>
                  <%= link_to 'Sign Up', signup_path %> or 
                  <%= link_to 'Log In', login_path %>
                <% end %>
            </div>
        </div>
    </div>
</nav>